nav(class="nav navbar navbar-expand-lg navbar-dark bg-dark fixed-top ")
  a(id="menu-toggle" href="#" class="nav-link" style="color:white;width:100px") 
    i(class="fas fa-bars")
  .container
    button(class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation")
      span(class="navbar-toggler-icon")
    div(class="collapse navbar-collapse justify-content-between" id="navbarNav")
      ul(id="menu-horizontal" class="navbar-nav")
        li(class="nav-item")
          a(class="nav-link" href="#")
        li(class="nav-item dropdown")
          a(class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false") Prueba
          div(class="dropdown-menu" aria-labelledby="navbarDropdown")
            a(class="dropdown-item")
            .dropdown-divider
      ul(class="navbar-nav")
        li(class="nav-item")
        if !user 
          a(id="loginLink" class="nav-link",href="/api/auth/login") Entrar
        else
          a(class="nav-link",href="#") #{user.Nombre}
          a(id="logout" class="nav-link",href="#") Salir
        